{% extends 'partials/base.html.twig' %}

{% block content %}
    {{ page.content }}
    <div id="koulenga-home">
    {% for module in page.collection() %}
        <div id="{{ _self.pageLinkName(module.menu) }}"></div>
        {{ module.content }}
    {% endfor %}
    </div>
{% endblock %}
